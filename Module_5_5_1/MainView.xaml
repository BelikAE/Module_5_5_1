<Window x:Class="Module_5_5_1.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Module_5_5_1"
             mc:Ignorable="d" 
             Height="250" Width="300"
             Title="Индефикатор элемента">
    <StackPanel>
        <Label Content="Колличество труб"
           Margin="5"/>
        <Button Content="Выбрать" Command="{Binding SelectPipes}"
            Margin="5"/>
        <Label Content="Объем стен"
   Margin="5"/>
        <Button Content="Выбрать" Command="{Binding SelectWalls}"
    Margin="5"/>
        <Label Content="Колличество дверей"
   Margin="5"/>
        <Button Content="Выбрать" Command="{Binding SelectDoors}"
    Margin="5"/>
    </StackPanel>
</Window>
